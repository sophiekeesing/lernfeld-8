# Colima Configuration for Trainify Project
# This file ensures Docker socket is properly accessible for docker and docker-compose commands

export DOCKER_HOST=unix://${HOME}/.colima/docker.sock
export DOCKER_CONTEXT=colima
